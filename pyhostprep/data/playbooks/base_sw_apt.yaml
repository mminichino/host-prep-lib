---
- hosts: localhost
  gather_facts: no
  name: Install
  tasks:
    - name: Packages
      ansible.builtin.package:
        name:
          - jq
          - wget
          - vim
          - maven
          - sysstat
          - apt-utils
          - ca-certificates
          - curl
          - gnupg
          - lsb-release
          - chrony
          - procps
          - zip
        state: latest
